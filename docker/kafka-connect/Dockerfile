# Kafka Connect worker with the Debezium PostgreSQL connector.
# Apicurio converters are bundled by the Debezium image and enabled at runtime.
ARG DEBEZIUM_VERSION=3.6.1.Final
FROM quay.io/debezium/connect:${DEBEZIUM_VERSION}
